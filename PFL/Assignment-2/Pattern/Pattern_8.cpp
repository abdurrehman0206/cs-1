#include <iostream>

using namespace std;

int main()
{
    int i, j, range, cnt, rtemp;
    cout << "Enter Range :: ";
    cin >> range;
    rtemp = range;
    for (i = 1; i <= range * 2; i++)
    {

        for (j = 1; j <= range; j++)
        {
        }
        cout << endl;
    }
    system("pause");
    return 0;
}